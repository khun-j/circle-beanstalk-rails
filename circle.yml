dependencies:
  pre:
    - pip install awsebcli
deployment:
  production:
    branch: master
    commands:
      - bash ./config-aws.sh
      - SECRET_KEY_BASE=$SECRET_KEY_BASE eb deploy
      - echo $SECRET_KEY_BASE
  staging:
    branch: staging
    commands: 
      - bash ./config-aws.sh
      - eb deploy
