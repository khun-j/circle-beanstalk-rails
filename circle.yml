dependencies:
  pre:
    - pip install awsebcli
deployment:
  production:
    branch: master
    commands:
      - bash ./config-aws.sh
      - eb deploy
  staging:
    branch: master
    commands: 
      - bash ./config-aws.sh
      - eb deploy
