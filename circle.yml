dependencies:
  pre:
    - pip install awsebcli
deployment:
  production:
    branch: master
    commands:
      - bash ./config-aws.sh
      - eb deploy
  staging:
    branch: staging
    commands: 
      - bash ./config-aws.sh
      - eb deploy
